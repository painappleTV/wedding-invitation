# 500 Internal Server Error 対処手順

API が「データの取得に失敗しました」と表示され、CloudWatch で 500 エラーが出る場合の対処手順です。

---

## ステップ1: CloudWatch でエラー内容を確認する

1. **AWS コンソール**にログインし、右上のリージョンが **アジアパシフィック（大阪） ap-northeast-3** になっているか確認
2. 検索バーで「**CloudWatch**」と入力し、CloudWatch を開く
3. 左メニューの「**ログ**」→「**ロググループ**」をクリック
4. 検索ボックスに「**GetGuest**」と入力
5. `/aws/lambda/●●●-GetGuestFunction-●●●` のような名前のロググループをクリック
6. 最新のログストリーム（一番上）をクリック
7. エラー発生時刻付近のログを確認し、**ERROR** や **error**、スタックトレースを探す

**よく出るエラーの例：**
- `ResourceNotFoundException` → DynamoDB テーブルが見つからない（リージョン違いの可能性）
- `AccessDeniedException` → Lambda に DynamoDB の権限がない
- `Cannot read property 'xxx' of undefined` → データ形式の問題

---

## ステップ2: リージョンの確認

Lambda と DynamoDB が同じリージョンにある必要があります。

1. **Lambda のリージョン確認**
   - AWS コンソールで「Lambda」を開く
   - 右上のリージョンが **ap-northeast-3（大阪）** か確認
   - 関数一覧に `wedding-invitation-GetGuestFunction` などがあるか確認

2. **DynamoDB のリージョン確認**
   - AWS コンソールで「DynamoDB」を開く
   - 右上のリージョンが **ap-northeast-3（大阪）** か確認
   - 左メニュー「テーブル」→ `wedding-guests` が存在するか確認

3. **リージョンが違う場合**
   - どちらか一方だけ別リージョンにあると 500 エラーになります
   - **対処**: `sam deploy` と シードスクリプトを同じリージョンで実行し直す

---

## ステップ3: Lambda に DynamoDB 権限を付与する

※ `template.yaml` にはすでに DynamoDB 権限（Policies）が追加済みです。

**やること**: 権限を反映させるため、ステップ5 で再デプロイするだけです。

---

## ステップ4: データの再投入

DynamoDB にデータがない、または形式が壊れている場合の対処です。

1. **PowerShell でシードスクリプトを実行**（ap-northeast-3 にデータ投入）

```powershell
cd C:\Users\super\wedding-invitation\scripts
$env:AWS_REGION = "ap-northeast-3"
npx tsx seed-guests.ts
```

2. **実行結果を確認**
   - `Added: 山田家 -> /invite/xxxxx` のように表示されれば成功
   - 表示された招待コード（xxxxx）を控えておく

3. **DynamoDB で確認**
   - DynamoDB コンソール → テーブル → wedding-guests → 「テーブルを探索」
   - 該当の inviteCode でアイテムが存在するか確認

---

## ステップ5: バックエンドの再デプロイ

変更を反映するために再デプロイします。

```powershell
cd C:\Users\super\wedding-invitation
sam build
sam deploy
```

- 初回は `sam deploy --guided` で設定
- 2回目以降は `sam deploy` だけでOK
- デプロイ後に表示される `ApiUrl` を控える

---

## ステップ6: 動作確認

1. ブラウザで次の URL を開く（ApiUrl と招待コードを実際の値に置き換え）

```
https://h4oqx4904i.execute-api.ap-northeast-3.amazonaws.com/Prod/guests/vj0diqo9
```

2. **JSON が表示されれば成功**
3. 表示されなければ、ステップ1 に戻り CloudWatch のエラーを再確認

---

## よくある原因まとめ

| 原因 | 対処 |
|------|------|
| リージョン不一致 | sam deploy とシードを同じリージョン（ap-northeast-3）で実行 |
| DynamoDB 権限不足 | template.yaml に Policies を追加して再デプロイ |
| データが空 | シードスクリプトを ap-northeast-3 で再実行 |
| テーブル名の不一致 | TABLE_NAME 環境変数が wedding-guests になっているか確認 |

---

CloudWatch のエラーメッセージが分かれば、より具体的な対処ができます。ログの内容をコピーして共有してください。
